# The repo root ignores only /node_modules (root-anchored), so the agent's own
# dependency tree and build output need ignoring explicitly.
app/*/node_modules/
app/*/dist/
app/*/node_modules.pnpmbak/
